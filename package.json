{
  "name": "myiworlds",
  "private": true,
  "scripts": {
    "watch": "lerna run watch --parallel",
    "build": "lerna run build --parallel",
    "generate": "lerna run generate --scope=@myiworlds/frontend",
    "frontend-build": "lerna run build --scope=@myiworlds/frontend",
    "backend": "lerna run watch --scope=@myiworlds/backend",
    "new-version": "lerna version --conventional-commits --yes",
    "fix": "npx lerna clean -y && npx lerna bootstrap --hoist",
    "bootstrap": "lerna bootstrap",
    "diff": "lerna diff",
    "update-apollo-tracing": "npx apollo service:push --scope=@myiworlds/graphql-api"
  },
  "devDependencies": {
    "lerna": "^3.16.4"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "dependencies": {}
}
